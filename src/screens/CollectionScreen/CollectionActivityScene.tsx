import { View, Text } from 'react-native';
import { memo } from 'react';

const CollectionActivityScene = () => {
  return (
    <View>
      <Text>CollectionActivityScene</Text>
    </View>
  );
};

export default memo(CollectionActivityScene);
